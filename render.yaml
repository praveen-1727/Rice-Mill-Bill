services:
  - type: web
    name: rice-mill-billing
    env: node
    buildCommand: cd server && npm install && cd ../rice-mill-billing-main && npm install --include=dev && npm run build
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
